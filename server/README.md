# Серверная часть (Backend)

## 🚀 Быстрый старт

### Предварительные требования

- Node.js (версия 24.8.0 или выше)
- MySQL сервер (версия 8.0 или выше)

### 1. Установка зависимостей

npm install

### Настройка переменных окружения

1. Переименуйте файл `.env.example` в `.env`
2. Отредактируйте .env файл, указав свои данные (в скобках примерный формат)

### ВАЖНО! перед запуском необходимо создать базу данных

### Запуск сервера

- **npm run start** - для запуска сервера
- **npm test** - для тестов
- **npm run dev** - для запуска в режиме разработки (nodemon)

## API Endpoint

### 🔐 Аутентификация

- **POST /api/user/registration** - Регистрация
- **POST /api/user/login** - Вход
- **POST /api/user/refresh** - Обновление токена
- **GET /api/user/check** - Проверка токена доступа
- **DELETE /api/user/logout/:tokenId** - Выход

### 📋 Задачи

- **POST /api/task** - Создать задачу
- **GET /api/task** - Получить все задачи
- **GET /api/task/:id** - Получить одну задачи
- **PUT /api/task/:id** - Изменить задачу
- **DELETE /api/task/:id** - Удалить задачу

### 📂 Группы

- **POST /api/group** - Создать группу
- **GET /api/group** - Получить все группы
- **PUT /api/group/:id** - Изменить группу
- **DELETE /api/group/:id** - Удалить группу
